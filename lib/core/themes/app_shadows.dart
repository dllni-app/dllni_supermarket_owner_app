import 'package:flutter/material.dart';

class AppShadows {
  const AppShadows._();
  static const BoxShadow shadow = BoxShadow(
    offset: Offset(0, 1),
    blurRadius: 2,
    color: Color(0x0D000000),
  );
}
